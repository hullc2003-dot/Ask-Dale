def is_proposal_safe(proposal_text: str, persona_id: str, db) -> bool:
    # Placeholder: run through safety rules, boundaries, maybe a small model
    # For now, just a stub that always returns True
    return True

