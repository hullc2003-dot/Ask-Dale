supabase/
  functions/
    agent/
      main.py
      providers.py
      requirements.txt
import os
from supabase import create_client
# 1. Connect to Supabase
url = "https://drmshuxoshnikrzudzto.supabase. we co"
key = os.environ.get("SUPABASE_SERVICE_ROLE_KEY")
supabase = create_client(url, key)
# 2. The "All-In-One" Secret Grabber
vault_keys = ["openai_api_key", "groq_api_key", "gemini_api_key"]
secrets = {k: supabase.rpc("get_vault_secret", {"p_name": k}).execute().data for k in vault_keys}
# 3. Assign them to your variables
OPENAI_KEY = secrets.get("openai_api_key")
GROQ_KEY = secrets.get("groq_api_key")
GEMINI_KEY = secrets.get("gemini_api_key")

https://drmshuxoshnikrzudzto.supabase.co
